<h1>All articles</h1>

<h2 class ="create_button"><%= link_to "Create New Article", new_article_path %></h2>

<table>
  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= link_to article.title, article %></td>
        <td><%= link_to 'Update', edit_article_path(article)%></td>
        <td><%= link_to 'Delete', article_path(article), method: :delete, data: { confirm: 'Are you sure?' } %></td></h4>
      </tr>
  </tbody>

    <% end %>
</table>